   #include <stdio.h>
   #include <stdlib.h>
    
 void main (void)
{
  char i;
  for (i=0;i<10;i++)
     printf (" Execultando dentro do for pela %d vez. \n ",i+1);
 printf ("O valor do i é %d. \n",i); 
 i=0;
 while (i>0)
     printf ("Execultado dentro do while pela %d vez \n",11- i--); 
  printf ("O valor do i é %d. \n",i);
 i=10;
 do 
   printf ("execultando do / while pela  %d vez \n",++i);
 while (i<10) ;
}
